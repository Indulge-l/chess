spring:
  application:
    name: chess-ai-provider
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka1:7301/eureka
  instance:
    preferIpAddress: true
    instance-id: chess-ai-provider_1
    lease-expiration-duration-in-seconds: 90
    lease-renewal-interval-in-seconds: 30